<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true">
        <Button
            android:id="@+id/btnAfficherTaches"
            android:layout_width="200sp"
            android:layout_height="wrap_content"
            android:text="Mes tâches"
            android:padding="5dp"
            android:textSize="18sp"
            android:drawableLeft="@drawable/task_24"
            android:backgroundTint="#F86F03"/>
        <Button
            android:id="@+id/btnAfficherNotes"
            android:layout_width="200sp"
            android:layout_height="wrap_content"
            android:layout_below="@+id/btnAfficherTaches"
            android:padding="5dp"
            android:textSize="18sp"
            android:text="Mes Notes"
            android:drawableLeft="@drawable/notes_24"
            android:backgroundTint="#F86F03"/>
    </LinearLayout>
</RelativeLayout>